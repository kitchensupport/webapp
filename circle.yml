general:
    branches:
        ignore:
            - gh-pages
machine:
    node:
        version: 4.1.1
deployment:
    gh_pages:
        branch: master
        commands:
            - chmod +x deploy.sh
            - ./deploy.sh
